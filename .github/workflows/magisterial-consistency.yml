name: Consistency-Check-Circa-A
on: [push, pull_request]

jobs:
  lithic-stabilization:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Substrate
        uses: actions/checkout@v4
      
      - name: Enforce CORE_DOGMA & Ex o Shield
        run: |
          echo "Initiating MLAOS Audit..."
          # Running mlaos_engine validation against current context
          python3 mlaos_engine.py
          # Sentinel check for explosive logic
          python3 logic_storm.py
